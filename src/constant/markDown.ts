import {
  darcula,
  oneDark,
  prism,
  vs,
} from "react-syntax-highlighter/dist/esm/styles/prism";

// 主题枚举
export enum ThemeEnum {
  DEFAULT = prism,
  ONEDARK = oneDark,
  Darcula = darcula,
  VS = vs,
}
